@page
@model gestionpresence.Areas.Admin.Pages.Sites.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>EcoleSite</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="EcoleSite.ID" />
            <div class="form-group">
                <label asp-for="EcoleSite.Libelle" class="control-label"></label>
                <input asp-for="EcoleSite.Libelle" class="form-control" />
                <span asp-validation-for="EcoleSite.Libelle" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Retour à la liste</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
